#include <stdio.h>
/**
 * main-prints out the given line
 * return:if the value is (0) then its a success
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
